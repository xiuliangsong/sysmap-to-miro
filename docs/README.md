# docs

`demo.png` — hero screenshot for the README: the `url-shortener` example
materialized on a Miro board by sysmap-to-miro (AWS icons + connectors + titled
zones). Regenerate by rendering `examples/url-shortener.architecture.json` onto a
board and exporting it.
